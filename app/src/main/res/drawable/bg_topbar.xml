<?xml version="1.0" encoding="utf-8"?>
<!-- Retro sunset gradient behind the glass address bar -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="@color/bg_top"
                android:endColor="@color/bg_bottom" />
        </shape>
    </item>
    <!-- neon horizon line -->
    <item android:gravity="bottom" android:height="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/neon_pink" />
        </shape>
    </item>
</layer-list>
